# DSA-basic
basic question of dsa easy to hard by numeber

Some random notes for hashing :-
*unorderd_map = mai element bss store hote hai;
*map = mai element store toh hote hee hai but ouput ascending order mai hota hai;

unorder_set = mai ek element ka copy nahi update hota;
set = mai v same hai bss order mai rahta hai;

overall (unorder for bara sai chota , noramal for chota sai bara);

